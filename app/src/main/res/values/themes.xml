<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RecordatorioMedico" parent="Theme.MaterialComponents.Light.NoActionBar">

    </style>

    <style name="Theme.RecordatorioMedico.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RecordatorioMedico.Transparent" parent="Theme.RecordatorioMedico.NoActionBar">
        <item name="android:windowBackground">@android:color/holo_blue_bright</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.RecordatorioMedico.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.RecordatorioMedico.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>
